import * as React from 'react';

interface IFieldCheckboxProps {
}

const FieldCheckbox: React.FC<IFieldCheckboxProps> = (props) => {
  return (
    <div></div>
  );
};

export default FieldCheckbox;
